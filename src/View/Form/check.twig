<input type="hidden" id="{{ postName }}" name="{{ postName }}" value="0"{% if disabled %} disabled{% endif %}>
<input type="checkbox" class="custom-check" id="{{ postName }}" name="{{ postName }}" placeholder="{{ placeholder }}"{% if checked %} checked{% endif %} value="1">
